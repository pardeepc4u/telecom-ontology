# Phase 2 — ontology validation + synthetic data generation
PyYAML>=6.0

# Phase 3 — ingestion (Neo4j graph load, Qdrant vector load, vLLM embeddings)
neo4j>=5.0
qdrant-client>=1.9
requests>=2.31
python-dotenv>=1.0

# Later phases add: fastapi, uvicorn.
